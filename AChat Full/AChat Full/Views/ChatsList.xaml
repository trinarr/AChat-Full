<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
  xmlns="http://xamarin.com/schemas/2014/forms"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  xmlns:vm="clr-namespace:AChatFull.Views"
  x:Class="AChatFull.Views.ChatsList"
  Title="Чаты">

    <ContentPage.BindingContext>
        <vm:ChatsViewModel />
    </ContentPage.BindingContext>

    <StackLayout>
        <ActivityIndicator
      IsRunning="{Binding IsBusy}"
      IsVisible="{Binding IsBusy}"
      HorizontalOptions="Center" />

        <CollectionView
      ItemsSource="{Binding Chats}"
      SelectionMode="Single"
      SelectionChanged="OnChatSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:ChatSummary">
                    <Grid Padding="10">
                        <Label Text="{Binding Title}" FontAttributes="Bold" />
                        <Label Text="{Binding LastMessage}" FontSize="Small" />
                        <Label Text="{Binding LastTimestamp, StringFormat='{0:HH:mm}'}" FontSize="Micro"
                   HorizontalOptions="End" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>